// process sirve para saber de donde estamos ejecutando la consulta o codigo. podemos saber en que ENV estamos utilizando

// console.log(process.argv);

// controla el proceso y su salida
// process.exit(1)

// podemos controlar eventos del proceso
// process.on( 'exit', () => {
//     //limpiar los recursos
// })

// platform
console.log(process.cwd())
